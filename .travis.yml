language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-emberate' ]; then cd .. && eval "mv $currentfolder generator-emberate" && cd generator-emberate; fi

